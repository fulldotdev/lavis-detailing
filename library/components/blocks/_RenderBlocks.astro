---
import { getBookshopConfig } from '@utils'
import RenderBlock from './_RenderBlock.astro'

const { blocks } = Astro.props

const mappedBlocks = blocks?.map((block: any) => ({
  ...block,
  cards: [
    block.features,
    block.highlights,
    block.offers,
    block.persons,
    block.products,
    block.services,
    block.reviews,
    block.posts,
    block.categories,
  ]
    .flat()
    .filter(Boolean),
}))
---

{
  mappedBlocks?.map((block: any) => {
    const bookshopConfig = getBookshopConfig(block._bookshop_name)
    return (
      <RenderBlock
        {...bookshopConfig}
        {...block}
      />
    )
  })
}
