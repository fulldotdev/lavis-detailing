title: Services

spec:
  structures:
    - content_blocks
  label: Services

blueprint:
  writeup:
  buttons:
  services:

config:
  section:
    align: start
    size:
  segment:
    layout: col
    align:
  buttons:
    importance: secondary
    layout: row
  deck:
    layout: carousel
    size:

_inputs:  
  blueprint:
    type: object
    options:
      subtype: mutable
      entries:
        allowed_keys:
          - writeup
          - buttons
          - categories

preview: