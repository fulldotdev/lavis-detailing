spec:
  structures:
    - content_blocks
  label: Custom Block

blueprint:

preview:

_inputs:
  blueprint:
    type: object
    options:
      subtype: mutable
      entries:
        structures: _structures.elements
        allowed_keys:
          - writeup
          - buttons
          - image
        assigned_structures:
          image:
            - image
          buttons:
            - buttons

_structures:
  elements:
    values:
      - id: image
        label: Image
        value:
          src:
          alt:
      - id: buttons
        value:
          - src:
          - alt:
