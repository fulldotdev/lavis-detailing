spec:
  structures:
    - content_blocks
  label: Categories

blueprint:
  writeup: Hello world

config:
  section:
    align: start
    size:
  segment:
    layout: col
    align:
  buttons:
    importance:
    layout:
  image:
    position:
    orientation:

preview:

_inputs:
  blueprint:
    type: object
    options:
      subtype: mutable
      entries:
        allowed_keys:
          - writeup
          - buttons
        # structures: _structures.elements

# _structures:
#   elements:
#     values:
#       - label: Image
#         value:
#           src:
#           alt:
