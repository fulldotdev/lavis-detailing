<!-- ---
import Image from '@components/elements/Image.astro'
import Writeup from '@components/elements/writeup/Writeup.astro'
import Card from '@components/wrappers/Card.astro'
import Segment from '@components/wrappers/segment/Segment.astro'
import type { Post,PostsCardsSettings } from '@types'
import { getSettingsCard } from 'api'

interface Props {
  card: Post
}

const { card } = Astro.props
const setting: PostsCardsSettings = await getSettingsCard('posts')
---

<Card
  {...setting?.card}
  href={card.path}
>
  <Image
    sizes={{ base: '590px' }}
    image={card.image}
    {...setting.image}
  />
  <Segment {...setting.segment}>
    <Writeup {...setting.writeup}>
      <h4 class="heading-secondary">{card.title}</h4>
      <p class="text-primary text-hue11">{card.description}</p>
    </Writeup>
  </Segment>
</Card> -->
