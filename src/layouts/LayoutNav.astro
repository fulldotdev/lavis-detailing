---
import CartSidebar from '@components/cart/CartSidebar.svelte'
import RenderBlocks from '@components/renderers/RenderBlocks.astro'
import Layout from '@layouts/Layout.astro'

const { seo, header_blocks, footer_blocks } = Astro.props
---

<Layout {seo}>
  <RenderBlocks blocks={header_blocks} />
  <!-- <CartSidebar client:load /> -->
  <main>
    <slot />
  </main>
  <RenderBlocks blocks={footer_blocks} />
</Layout>
