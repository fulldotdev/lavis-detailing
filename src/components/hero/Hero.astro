---
const { title, description, layout = false } = Astro.props
---

<!-- bg-blue -->
<section class:list={[{ 'bg-red flex': layout }, title, description]}>
  <p>{Astro.props.title}</p>
  <p>
    <p>{Astro.props.description}</p>
  </p>
</section>
