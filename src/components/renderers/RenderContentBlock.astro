---
import Writeup from '@components/elements/Writeup.astro'
import { ButtonProps } from '@components/elements/button/Button.astro'
import Buttons from '@components/elements/button/Buttons.astro'
import Channels from '@components/elements/channel/Channels.astro'
import Image, { ImageProps } from '@components/elements/image/Image.astro'
import Socials from '@components/elements/social/Socials.astro'
import type { RenderCardsItem } from '@components/renderers/RenderCards.astro'
import RenderCards from '@components/renderers/RenderCards.astro'
import Deck, { type DeckProps } from '@components/wrappers/Deck.astro'
import Section, { type SectionProps } from '@components/wrappers/Section.astro'
import Segment, { type SegmentProps } from '@components/wrappers/Segment.astro'

interface Props {
  section?: SectionProps
  segment?: SegmentProps
  writeup?: string
  buttons?: ButtonProps[]
  channels?: boolean
  socials?: boolean
  image?: ImageProps | null
  deck?: DeckProps
  features?: RenderCardsItem[]
}

const {
  section,
  segment,
  writeup,
  buttons,
  image,
  channels,
  socials,
  deck,
  features,
} = Astro.props
---

<Section {...section}>
  <Segment {...segment}>
    <Writeup set:html={writeup} />
    <Buttons {buttons} />
    <Channels {channels} />
    <Socials {socials} />
  </Segment>
  <Image {...image} />
  <Deck {...deck}>
    <RenderCards items={features} />
  </Deck>
</Section>
