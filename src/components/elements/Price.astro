---
import { getEntry } from 'astro:content'
import { cva } from 'cva'

const variant = cva({})
const elements = await getEntry('globals', 'elements')
const { price } = Astro.props
---

<span class={variant(elements?.data.price)}> €{price.toFixed(2)}</span>
