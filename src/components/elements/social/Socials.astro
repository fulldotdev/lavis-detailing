---
import { getEntry } from 'astro:content'
import Social from './Social.astro'

interface props {
  exclude: boolean
}

const { exclude } = Astro.props
const company = await getEntry('data', 'company')
---

{!exclude && company.data.socials?.map((social: any) => <Social {...social} />)}
