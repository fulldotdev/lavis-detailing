---
import type { SocialProps } from './Social.astro'
import Social from './Social.astro'

type Props = SocialProps & {
  socials: SocialProps[]
}

const { socials, ...restProps } = Astro.props
---

{
  socials?.map((social) => (
    <Social
      {...social}
      {...restProps}
    />
  ))
}
