---
import { getEntry } from 'astro:content'
import Channel from './Channel.astro'

interface props {
  exclude: boolean
}

const { exclude } = Astro.props
const company = await getEntry('data', 'company')
---

{
  !exclude &&
    company.data.channels?.map((channel: any) => <Channel {...channel} />)
}
