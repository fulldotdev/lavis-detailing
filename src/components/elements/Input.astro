---
// interface Props {
//   input: {
//     label: string
//     type?: 'text' | 'email' | 'number' | 'password' | 'tel' | 'url'
//     required?: boolean
//   }
//   class?: string
// }

// const { input, class: className } = Astro.props
---

<!-- {
  typeof input === 'object' && (
    <div
      class="flex w-full flex-col"
      class:list={className}
    >
      <label
        class="label"
        for={input.label}
      >
        {input.label}
      </label>
      <input
        class="input"
        name={input.label}
        id={input.label}
        type={input.type || 'text'}
      />
    </div>
  )
} -->
