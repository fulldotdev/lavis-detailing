---
import BrandsCard from './BrandsCard.astro'

interface Props {
  brands: string[]
}

// const entries = await getCollection('brands', (entry: any) => {
//   brands.includes(entry.slug)
// })

const { brands } = Astro.props
---

<!-- {entries?.map((entry) => <BrandsCard brand={entry} />)} -->
