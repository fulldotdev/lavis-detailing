---
import type { FeaturesCardProps } from './FeaturesCard.astro'
import FeaturesCard from './FeaturesCard.astro'

interface Props {
  features: FeaturesCardProps[]
}

const { features } = Astro.props
---

{features?.map((feature) => <FeaturesCard {...feature} />)}
